package models.worldModels;

import assets.material.Material;
import models.seeds.ColorFunction;

public class SeaMesh extends TriangleMesh {

	public SeaMesh(float edgeLength, float[][] elevation, ColorFunction colorFunc, Material material) {
		super(edgeLength, elevation, colorFunc, material);
		// TODO Auto-generated constructor stub
	}

}
